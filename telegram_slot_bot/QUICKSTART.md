# 🚀 Быстрый старт - Telegram Slot Bot

## За 5 минут до запуска!

### Шаг 1: Создание бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Введите имя: `Lucky Slot Bot`
5. Введите username: `your_lucky_slot_bot`
6. **Скопируйте токен!** Он выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Шаг 2: Узнайте свой User ID (30 секунд)

1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. **Скопируйте ваш ID!** (например: `123456789`)

### Шаг 3: Установка PostgreSQL (2 минуты)

#### Windows:
```bash
# Скачайте и установите с postgresql.org
# Или через Chocolatey:
choco install postgresql
```

#### Linux:
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
```

#### Mac:
```bash
brew install postgresql
brew services start postgresql
```

### Шаг 4: Создание базы данных (1 минута)

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# В консоли PostgreSQL выполните:
CREATE DATABASE slot_bot_db;
CREATE USER slot_bot_user WITH PASSWORD 'mypassword123';
GRANT ALL PRIVILEGES ON DATABASE slot_bot_db TO slot_bot_user;
\q
```

### Шаг 5: Настройка проекта (2 минуты)

```bash
# Перейдите в папку проекта
cd telegram_slot_bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте (Windows)
venv\Scripts\activate

# Активируйте (Linux/Mac)
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 6: Конфигурация (1 минута)

Создайте файл `.env` и вставьте:

```env
TELEGRAM_BOT_TOKEN=ВАШТОКЕНОТBOTFATHER
DATABASE_URL=postgresql://slot_bot_user:mypassword123@localhost:5432/slot_bot_db
INITIAL_BALANCE=1000
SPIN_COST=35
ADMIN_IDS=ВАШUSERID
STARS_CONVERSION_RATE=10
```

**Замените:**
- `ВАШТОКЕНОТBOTFATHER` - на токен от BotFather
- `ВАШUSERID` - на ваш Telegram ID
- `mypassword123` - на ваш пароль от PostgreSQL

### Шаг 7: Запуск! (10 секунд)

```bash
python bot.py
```

Вы должны увидеть:
```
INFO - 🎰 Lucky Slot Bot запущен!
```

### Шаг 8: Тестирование (1 минута)

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Попробуйте `/spin`

🎉 **Готово!** Ваш бот работает!

---

## 🔥 Быстрая проверка

### Проверка 1: Бот отвечает?
```
/start
```
Должно прийти приветственное сообщение.

### Проверка 2: Слот работает?
```
/spin
```
Должен появиться анимированный слот.

### Проверка 3: База данных работает?
```
/balance
```
Должен показать ваш баланс.

### Проверка 4: Админ команды работают?
```
/adminstats
```
Должна показаться глобальная статистика.

---

## ❗ Частые проблемы

### Проблема: "Database connection error"

**Решение:**
```bash
# Проверьте, что PostgreSQL запущен
# Windows:
net start postgresql

# Linux:
sudo systemctl status postgresql

# Mac:
brew services list
```

### Проблема: "Unauthorized"

**Решение:**
Проверьте токен в `.env` файле. Он должен быть без кавычек и пробелов.

### Проблема: "Module not found"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
# Должно быть (venv) в начале строки

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте, что `bot.py` запущен
2. Проверьте логи на наличие ошибок
3. Убедитесь, что интернет соединение работает
4. Попробуйте `/start` в боте

---

## 🎯 Следующие шаги

После запуска:

1. **Протестируйте все команды:**
   - `/start`, `/spin`, `/balance`, `/stats`
   - `/achievements`, `/top`, `/help`

2. **Настройте Telegram Stars:**
   - В [@BotFather](https://t.me/BotFather) → `/mybots`
   - Выберите бота → `Bot Settings` → `Payments`
   - Подключите Telegram Stars

3. **Определите dice_value комбинации:**
   ```bash
   python dice_tester.py
   ```
   - Отправьте `/test 100`
   - Вручную записывайте результаты
   - Обновите `DICE_TO_COMBO` в `game_logic.py`

4. **Измените настройки под себя:**
   - Таблица выплат в `game_logic.py`
   - Стоимость спина в `.env`
   - Пакеты покупки в `bot.py`

5. **Деплой на сервер:**
   - Следуйте инструкциям в `README.md`

---

## 📚 Полезные команды

```bash
# Просмотр логов в реальном времени
python bot.py

# Запуск в фоне (Linux/Mac)
nohup python bot.py > bot.log 2>&1 &

# Остановка (найти процесс)
ps aux | grep bot.py
kill <PID>

# Обновление зависимостей
pip install --upgrade -r requirements.txt

# Бэкап базы данных
pg_dump -U slot_bot_user slot_bot_db > backup.sql

# Восстановление базы данных
psql -U slot_bot_user slot_bot_db < backup.sql
```

---

## 💡 Советы

1. **Всегда делайте бэкапы БД** перед обновлениями
2. **Тестируйте изменения** на тестовом боте
3. **Мониторьте логи** на наличие ошибок
4. **Записывайте статистику** для анализа RTP
5. **Обновляйте токены** если бот перестал работать

---

## 🆘 Нужна помощь?

1. Проверьте [README.md](README.md) для подробной информации
2. Проверьте логи: `tail -f bot.log`
3. Проверьте статус БД: `psql -U slot_bot_user -d slot_bot_db -c "SELECT COUNT(*) FROM users;"`

---

**Готово! Теперь у вас работающий Telegram Slot Bot! 🎰**
