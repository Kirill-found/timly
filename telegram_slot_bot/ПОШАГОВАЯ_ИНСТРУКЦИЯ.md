# 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ - Запуск Telegram Slot Bot

## ✅ ШАГ 1: Узнайте свой Telegram ID (2 минуты)

1. **Откройте Telegram**
2. **Найдите бота** [@userinfobot](https://t.me/userinfobot)
3. **Нажмите START** или отправьте любое сообщение
4. **Скопируйте ваш ID** (это будет число, например: 123456789)

📝 **Запишите ваш ID здесь:** _______________

---

## ✅ ШАГ 2: Установите PostgreSQL (5-10 минут)

### Для Windows:

**Вариант 1: Через установщик (рекомендуется)**

1. **Скачайте PostgreSQL:**
   - Откройте: https://www.postgresql.org/download/windows/
   - Нажмите "Download the installer"
   - Выберите версию 15 или 16
   - Скачайте и запустите установщик

2. **Во время установки:**
   - Запомните пароль, который вы вводите для пользователя `postgres`
   - Оставьте порт по умолчанию: 5432
   - Всё остальное - по умолчанию (Next, Next, Next)

3. **Проверьте установку:**
   - Откройте "Командную строку" (Win + R → `cmd`)
   - Введите: `psql --version`
   - Должна показаться версия PostgreSQL

📝 **Запишите пароль от PostgreSQL:** _______________

**Вариант 2: Через Docker (если умеете работать с Docker)**

```bash
docker run --name postgres-slot-bot -e POSTGRES_PASSWORD=mypassword -p 5432:5432 -d postgres:15
```

---

## ✅ ШАГ 3: Создайте базу данных (3 минуты)

### Вариант 1: Через pgAdmin (графический интерфейс)

1. **Откройте pgAdmin** (должен был установиться вместе с PostgreSQL)
2. **Подключитесь:**
   - Введите пароль от PostgreSQL
3. **Создайте базу данных:**
   - Правый клик на "Databases" → Create → Database
   - Name: `slot_bot_db`
   - Owner: postgres
   - Нажмите Save

4. **Создайте пользователя:**
   - Правый клик на "Login/Group Roles" → Create → Login/Group Role
   - General → Name: `slot_bot_user`
   - Definition → Password: `myslotbot123` (придумайте свой!)
   - Privileges → Can login? ✅ YES
   - Нажмите Save

5. **Дайте права:**
   - Правый клик на базу `slot_bot_db` → Properties
   - Security → Privileges → Add
   - Grantee: `slot_bot_user`
   - Privileges: ALL
   - Save

### Вариант 2: Через командную строку (быстрее)

1. **Откройте командную строку** (Win + R → `cmd`)

2. **Подключитесь к PostgreSQL:**
```bash
psql -U postgres
```

3. **Введите пароль от PostgreSQL**

4. **Выполните команды** (копируйте по одной строке):

```sql
CREATE DATABASE slot_bot_db;
```

```sql
CREATE USER slot_bot_user WITH PASSWORD 'myslotbot123';
```

```sql
GRANT ALL PRIVILEGES ON DATABASE slot_bot_db TO slot_bot_user;
```

```sql
\q
```

📝 **Запишите пароль от slot_bot_user:** _______________
(рекомендую: `myslotbot123`)

---

## ✅ ШАГ 4: Установите Python (если ещё нет)

1. **Проверьте, есть ли Python:**
   - Откройте командную строку
   - Введите: `python --version`
   - Если показывает версию 3.8+ → всё ОК, переходите к ШАГ 5

2. **Если Python нет:**
   - Скачайте с https://www.python.org/downloads/
   - **ВАЖНО:** При установке поставьте галочку "Add Python to PATH"
   - Установите

3. **Проверьте снова:**
```bash
python --version
```

---

## ✅ ШАГ 5: Подготовьте проект (5 минут)

1. **Откройте PowerShell или командную строку**

2. **Перейдите в папку проекта:**
```bash
cd "c:\Users\wtk_x\OneDrive\Рабочий стол\TIMLY\telegram_slot_bot"
```

3. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

4. **Активируйте виртуальное окружение:**

**Для PowerShell:**
```bash
venv\Scripts\Activate.ps1
```

**Если появляется ошибка про ExecutionPolicy:**
```bash
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
Затем снова:
```bash
venv\Scripts\Activate.ps1
```

**Для обычной командной строки (cmd):**
```bash
venv\Scripts\activate.bat
```

5. **Проверьте активацию:**
   - В начале строки должно появиться `(venv)`

6. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

Подождите 1-2 минуты, пока всё установится.

---

## ✅ ШАГ 6: Создайте файл .env (2 минуты)

1. **В папке проекта создайте файл `.env`** (можно через Блокнот)

2. **Скопируйте и вставьте туда это:**

```env
# Токен от BotFather
TELEGRAM_BOT_TOKEN=8486421685:AAE_qheWvj7pXGZNuax_s-uhVC50Fi3kUR0

# Подключение к PostgreSQL
# ЗАМЕНИТЕ myslotbot123 на ваш пароль, если другой!
DATABASE_URL=postgresql://slot_bot_user:myslotbot123@localhost:5432/slot_bot_db

# Игровые настройки
INITIAL_BALANCE=1000
SPIN_COST=35

# ЗАМЕНИТЕ на ваш Telegram ID из ШАГ 1!
ADMIN_IDS=ВАШ_TELEGRAM_ID_СЮДА

# Конверсия Telegram Stars
STARS_CONVERSION_RATE=10
```

3. **ВАЖНО! Замените:**
   - `ВАШ_TELEGRAM_ID_СЮДА` → на ваш ID из ШАГ 1
   - `myslotbot123` → на ваш пароль от PostgreSQL (если другой)

4. **Сохраните файл как `.env`** (именно с точкой в начале!)

### Пример готового .env файла:

```env
TELEGRAM_BOT_TOKEN=8486421685:AAE_qheWvj7pXGZNuax_s-uhVC50Fi3kUR0
DATABASE_URL=postgresql://slot_bot_user:myslotbot123@localhost:5432/slot_bot_db
INITIAL_BALANCE=1000
SPIN_COST=35
ADMIN_IDS=123456789
STARS_CONVERSION_RATE=10
```

---

## ✅ ШАГ 7: ЗАПУСТИТЕ БОТА! 🚀 (30 секунд)

1. **Убедитесь, что:**
   - ✅ PowerShell/cmd открыт
   - ✅ Вы в папке `telegram_slot_bot`
   - ✅ Виртуальное окружение активировано (видно `(venv)`)

2. **Запустите бота:**
```bash
python bot.py
```

3. **Вы должны увидеть:**
```
INFO - Database initialized successfully
INFO - 🎰 Lucky Slot Bot запущен!
```

**🎉 ПОЗДРАВЛЯЮ! БОТ РАБОТАЕТ!**

---

## ✅ ШАГ 8: Протестируйте бота (5 минут)

1. **Откройте Telegram**

2. **Найдите вашего бота:**
   - Поиск → введите username вашего бота
   - ИЛИ откройте чат с @BotFather и нажмите на ссылку бота

3. **Нажмите START**

4. **Попробуйте команды:**

```
/start       - Должно прийти приветствие
/balance     - Должен показать баланс 1000 ⭐️
/spin        - Должен крутиться слот!
/stats       - Статистика
/achievements - Достижения
/adminstats  - Админ статистика (только для вас)
```

5. **Сделайте 5-10 спинов:**
   - `/spin`
   - Подождите результат
   - Повторите

---

## ✅ ШАГ 9: Определите dice_value комбинации (опционально, но важно!)

Сейчас в боте используются ПРИМЕРНЫЕ значения для выигрышных комбинаций. Нужно определить точные!

### Вариант 1: Автоматическое тестирование

1. **Остановите бота** (Ctrl + C в консоли где он запущен)

2. **Запустите тестер:**
```bash
python dice_tester.py
```

3. **В Telegram отправьте боту:**
```
/test 50
```

Бот сделает 50 тестовых спинов и запишет все значения.

4. **Вручную записывайте визуальные результаты!**
   - Например: "dice_value 22 → 🍋🍋🍋"
   - "dice_value 64 → 7️⃣7️⃣7️⃣"

5. **Посмотрите анализ:**
```
/analyze
```

6. **Экспортируйте данные:**
```
/export
```

Бот отправит CSV файл со статистикой.

### Вариант 2: Ручное тестирование

1. **Запустите основного бота:**
```bash
python bot.py
```

2. **Делайте спины и записывайте:**
   - В консоли будет показан `dice_value`
   - В Telegram видно визуальный результат
   - Записывайте соответствия

3. **После 50-100 спинов обновите `game_logic.py`:**

Откройте файл `game_logic.py` и найдите:

```python
DICE_TO_COMBO = {
    1: "🍒🍒🍒",      # Замените на реальные значения!
    22: "🍋🍋🍋",     # Замените на реальные значения!
    43: "🍇🍇🍇",     # Замените на реальные значения!
    64: "7️⃣7️⃣7️⃣",    # Замените на реальные значения!
}
```

Замените цифры на те, которые вы записали.

---

## ✅ ШАГ 10: Настройте автозапуск (опционально)

### Чтобы бот запускался автоматически при включении компьютера:

1. **Создайте bat файл** `start_bot.bat` в папке проекта:

```batch
@echo off
cd /d "c:\Users\wtk_x\OneDrive\Рабочий стол\TIMLY\telegram_slot_bot"
call venv\Scripts\activate.bat
python bot.py
pause
```

2. **Создайте ярлык** этого файла

3. **Добавьте в автозагрузку:**
   - Win + R → `shell:startup`
   - Скопируйте туда ярлык

Теперь бот будет запускаться автоматически!

---

## 🔧 Решение проблем

### Проблема 1: "Database connection error"

**Решение:**
```bash
# Проверьте, что PostgreSQL запущен
# Откройте "Службы" (Win + R → services.msc)
# Найдите "postgresql" и убедитесь что запущена

# ИЛИ в командной строке:
net start postgresql-x64-15
```

### Проблема 2: "ModuleNotFoundError"

**Решение:**
```bash
# Убедитесь что виртуальное окружение активировано
# Должно быть (venv) в начале строки

# Переустановите зависимости
pip install --upgrade -r requirements.txt
```

### Проблема 3: Бот не отвечает в Telegram

**Решение:**
1. Проверьте, что `bot.py` запущен (в консоли должно быть "Бот запущен!")
2. Проверьте токен в `.env` файле
3. Проверьте интернет соединение
4. Попробуйте перезапустить бота (Ctrl+C и снова `python bot.py`)

### Проблема 4: "Permission denied" при создании .env

**Решение:**
- Создайте файл через Блокнот
- "Файл" → "Сохранить как"
- Имя файла: `.env` (с точкой!)
- Тип файла: "Все файлы"

### Проблема 5: PostgreSQL не запускается

**Решение:**
```bash
# Проверьте порт 5432 (возможно занят)
netstat -ano | findstr :5432

# Или используйте Docker вместо установки PostgreSQL
```

---

## 📝 Чеклист "Всё работает"

Отметьте галочкой, когда выполните:

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `slot_bot_db` создана
- [ ] Пользователь `slot_bot_user` создан
- [ ] Python установлен (версия 3.8+)
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и заполнен правильно
- [ ] Ваш Telegram ID добавлен в `ADMIN_IDS`
- [ ] Бот запустился без ошибок
- [ ] В Telegram бот отвечает на `/start`
- [ ] Команда `/spin` работает
- [ ] Команда `/adminstats` показывает статистику

---

## 🎯 Что дальше?

После успешного запуска:

1. **Протестируйте все команды** (см. [CHEATSHEET.md](CHEATSHEET.md))

2. **Определите dice_value комбинации** (ШАГ 9)

3. **Настройте Telegram Stars** для монетизации:
   - [@BotFather](https://t.me/BotFather) → `/mybots`
   - Выберите бота → `Bot Settings` → `Payments`
   - Подключите Telegram Stars

4. **Измените настройки под себя:**
   - Таблица выплат → `game_logic.py`
   - Стоимость спина → `.env` файл
   - Пакеты покупки → `bot.py`

5. **Деплой на сервер** (когда будете готовы):
   - Инструкции в [README.md](README.md)

---

## 📞 Нужна помощь?

1. **Проверьте логи** - в консоли где запущен бот
2. **Проверьте [CHEATSHEET.md](CHEATSHEET.md)** - там решения частых проблем
3. **Проверьте [README.md](README.md)** - полная документация

---

## 🎉 Поздравляю!

Если вы дошли до конца и бот работает - вы молодец!

У вас теперь есть **полнофункциональный коммерческий Telegram-бот** с:
- ✅ Слот-машиной
- ✅ Системой достижений (16 штук!)
- ✅ Интеграцией Telegram Stars
- ✅ Админ-панелью
- ✅ PostgreSQL базой данных
- ✅ Полной статистикой

**Удачи в развитии вашего проекта! 🚀🎰**
