# 🎰 Telegram Slot Bot - Коммерческий проект

Полнофункциональный Telegram-бот со слот-машиной, системой достижений и монетизацией через Telegram Stars.

## 📋 Особенности

✅ **Игровая механика**
- Слот-машина на базе Telegram Dice API
- Таблица выплат с джекпотом
- Статистика и RTP

✅ **Монетизация**
- Интеграция Telegram Stars
- Покупка игровых звёзд
- Гибкая система пакетов

✅ **Система достижений**
- 16 уникальных достижений
- Автоматическое отслеживание прогресса
- Награды за достижения

✅ **Админ-панель**
- Глобальная статистика
- Управление пользователями
- Бан/разбан
- Выдача бонусов

✅ **База данных PostgreSQL**
- Полная история транзакций
- Статистика пользователей
- Платежи

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонируйте репозиторий
cd telegram_slot_bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка PostgreSQL

```bash
# Установите PostgreSQL (если ещё не установлен)
# Windows: Скачайте с postgresql.org
# Linux: sudo apt install postgresql postgresql-contrib
# Mac: brew install postgresql

# Создайте базу данных
psql -U postgres
CREATE DATABASE slot_bot_db;
CREATE USER slot_bot_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE slot_bot_db TO slot_bot_user;
\q
```

### 3. Создание бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям:
   - Имя бота: "Lucky Slot Bot"
   - Username: `lucky_slot_bot` (должен заканчиваться на `bot`)
4. Скопируйте токен

### 4. Настройка окружения

Создайте файл `.env` из примера:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Токен от BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Подключение к PostgreSQL
DATABASE_URL=postgresql://slot_bot_user:your_password@localhost:5432/slot_bot_db

# Игровые настройки
INITIAL_BALANCE=1000
SPIN_COST=35

# ID администраторов (ваш Telegram User ID)
ADMIN_IDS=123456789

# Конверсия Telegram Stars в игровые звёзды
# 1 Telegram Star = 10 игровых звёзд
STARS_CONVERSION_RATE=10
```

### 5. Получение вашего Telegram User ID

Найдите [@userinfobot](https://t.me/userinfobot) в Telegram и отправьте любое сообщение. Бот вернёт ваш ID.

### 6. Запуск бота

```bash
python bot.py
```

Бот запустится и будет доступен в Telegram!

## 📱 Команды пользователя

- `/start` - Начать работу с ботом
- `/spin` - Крутить слот (35 ⭐️)
- `/balance` - Проверить баланс
- `/stats` - Статистика игрока
- `/achievements` - Просмотр достижений
- `/top` - Топ-10 игроков
- `/buy` - Купить звёзды
- `/help` - Справка

## 🔧 Админ команды

Доступны только пользователям из `ADMIN_IDS`:

- `/adminstats` - Глобальная статистика
- `/ban <user_id> [причина]` - Заблокировать пользователя
- `/unban <user_id>` - Разблокировать пользователя
- `/givestars <user_id> <amount>` - Выдать звёзды пользователю

## 🏆 Система достижений

Всего 16 достижений:

### Игровые достижения
- 🎰 **Первый спин** - Сделать 1 спин (50 ⭐️)
- 🌟 **Новичок** - Сделать 10 спинов (100 ⭐️)
- ⭐ **Опытный** - Сделать 100 спинов (500 ⭐️)
- 💫 **Ветеран** - Сделать 1000 спинов (2000 ⭐️)

### Достижения за победы
- 🎉 **Первая победа** - Выиграть первый раз (100 ⭐️)
- 🍀 **Везунчик** - Выиграть 10 раз (300 ⭐️)
- 🎊 **Счастливчик** - Выиграть 100 раз (1000 ⭐️)
- 💎 **Джекпот!** - Получить 7️⃣7️⃣7️⃣ (500 ⭐️)
- 🔥 **Большой куш** - Выиграть 500+ за спин (500 ⭐️)

### Финансовые достижения
- 💸 **Щедрый** - Потратить 10,000 звёзд (1000 ⭐️)
- 💰 **Магнат** - Потратить 100,000 звёзд (5000 ⭐️)
- 🤑 **Богач** - Накопить 10,000 звёзд (1000 ⭐️)
- 👑 **Миллионер** - Накопить 100,000 звёзд (10000 ⭐️)

### Мета-достижения
- 📅 **Преданный** - Играть 7 дней подряд (700 ⭐️)
- 🏆 **Охотник за достижениями** - Получить 5 достижений (500 ⭐️)
- 📚 **Коллекционер** - Получить все достижения (5000 ⭐️)

## 💎 Telegram Stars - Монетизация

### Пакеты покупки

Настроены 5 пакетов:
- 💎 100 звёзд = 10 Telegram Stars
- 💎 500 звёзд = 50 Telegram Stars
- 💎 1000 звёзд = 100 Telegram Stars
- 💎 5000 звёзд = 500 Telegram Stars
- 💎 10000 звёзд = 1000 Telegram Stars

### Настройка Telegram Stars

1. В [@BotFather](https://t.me/BotFather) отправьте `/mybots`
2. Выберите вашего бота
3. `Bot Settings` → `Payments`
4. Подключите Telegram Stars

**ВАЖНО:** Для работы с Telegram Stars бот должен быть проверен и одобрен Telegram.

## 🧪 Тестирование Dice API

Для определения точного соответствия `dice_value` реальным комбинациям используйте тестовый модуль:

```bash
python dice_tester.py
```

### Команды тестера:
- `/test [количество]` - Запустить тест (по умолчанию 10 спинов)
- `/analyze` - Анализ собранной статистики
- `/export` - Экспорт статистики в CSV
- `/resetstats confirm` - Сброс статистики

### Как использовать:

1. Запустите `dice_tester.py`
2. Отправьте `/test 100` для 100 спинов
3. **Вручную записывайте визуальные результаты!**
4. Отправьте `/analyze` для анализа
5. Обновите `DICE_TO_COMBO` в `game_logic.py` на основе результатов

## 📊 Структура базы данных

### Таблицы:

- **users** - Пользователи бота
- **transactions** - История всех транзакций
- **achievements** - Определения достижений
- **user_achievements** - Связь пользователь-достижение
- **payments** - История платежей через Telegram Stars
- **game_stats** - Глобальная статистика
- **dice_value_mappings** - Статистика dice_value (для тестирования)

## 🌐 Деплой на продакшн

### Вариант 1: VPS сервер

```bash
# 1. Подключитесь к серверу
ssh user@your-server.com

# 2. Установите зависимости
sudo apt update
sudo apt install python3 python3-pip python3-venv postgresql

# 3. Создайте базу данных
sudo -u postgres psql
CREATE DATABASE slot_bot_db;
CREATE USER slot_bot_user WITH PASSWORD 'strong_password';
GRANT ALL PRIVILEGES ON DATABASE slot_bot_db TO slot_bot_user;
\q

# 4. Загрузите код
git clone your-repo-url
cd telegram_slot_bot

# 5. Настройте окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 6. Создайте .env файл
nano .env
# (вставьте настройки)

# 7. Создайте systemd service
sudo nano /etc/systemd/system/slot-bot.service
```

Содержимое `slot-bot.service`:

```ini
[Unit]
Description=Telegram Slot Bot
After=network.target postgresql.service

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/telegram_slot_bot
Environment="PATH=/path/to/telegram_slot_bot/venv/bin"
ExecStart=/path/to/telegram_slot_bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# 8. Запустите сервис
sudo systemctl daemon-reload
sudo systemctl enable slot-bot
sudo systemctl start slot-bot

# 9. Проверьте статус
sudo systemctl status slot-bot

# 10. Просмотр логов
sudo journalctl -u slot-bot -f
```

### Вариант 2: Docker

```bash
# Создайте Dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y postgresql-client

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

```bash
# Создайте docker-compose.yml
version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: slot_bot_db
      POSTGRES_USER: slot_bot_user
      POSTGRES_PASSWORD: your_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  bot:
    build: .
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgresql://slot_bot_user:your_password@postgres:5432/slot_bot_db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  postgres_data:
```

```bash
# Запустите
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot
```

### Вариант 3: Heroku

```bash
# 1. Создайте Procfile
echo "web: python bot.py" > Procfile

# 2. Создайте runtime.txt
echo "python-3.11.0" > runtime.txt

# 3. Инициализируйте git
git init
git add .
git commit -m "Initial commit"

# 4. Создайте Heroku app
heroku create your-slot-bot

# 5. Добавьте PostgreSQL
heroku addons:create heroku-postgresql:mini

# 6. Установите переменные окружения
heroku config:set TELEGRAM_BOT_TOKEN=your_token
heroku config:set SPIN_COST=35
heroku config:set ADMIN_IDS=your_id
heroku config:set STARS_CONVERSION_RATE=10

# 7. Деплой
git push heroku master

# 8. Проверьте логи
heroku logs --tail
```

## 🔒 Безопасность

### Важные рекомендации:

1. **Никогда не коммитьте `.env` файл!**
2. Используйте сильные пароли для PostgreSQL
3. Регулярно обновляйте зависимости
4. Включите SSL для подключения к БД в продакшене
5. Ограничьте доступ к PostgreSQL по IP
6. Регулярно делайте бэкапы базы данных

### Бэкап базы данных:

```bash
# Создать бэкап
pg_dump -U slot_bot_user slot_bot_db > backup_$(date +%Y%m%d).sql

# Восстановить из бэкапа
psql -U slot_bot_user slot_bot_db < backup_20231215.sql
```

## 📈 Мониторинг и аналитика

### Полезные запросы к БД:

```sql
-- Топ игроков
SELECT username, balance, total_spins, total_won
FROM users
ORDER BY balance DESC
LIMIT 10;

-- Статистика за сегодня
SELECT
    COUNT(*) as spins_today,
    SUM(amount) as total_wagered
FROM transactions
WHERE DATE(timestamp) = CURRENT_DATE
AND transaction_type = 'spin';

-- RTP за последние 7 дней
SELECT
    (SUM(CASE WHEN transaction_type = 'win' THEN amount ELSE 0 END) * 100.0 /
     SUM(CASE WHEN transaction_type = 'spin' THEN ABS(amount) ELSE 0 END)) as rtp_7d
FROM transactions
WHERE timestamp > NOW() - INTERVAL '7 days';
```

## ❓ FAQ

### Как изменить таблицу выплат?

Отредактируйте `PAYOUTS` в `game_logic.py`:

```python
PAYOUTS = {
    "🍒🍒🍒": 150,  # Было 100
    "🍋🍋🍋": 300,  # Было 200
    "🍇🍇🍇": 500,  # Было 350
    "7️⃣7️⃣7️⃣": 1000, # Было 500
}
```

### Как изменить стоимость спина?

В `.env` файле измените `SPIN_COST=35` на нужное значение.

### Как добавить новые достижения?

Добавьте в список `ACHIEVEMENTS` в `achievements.py` и реализуйте логику проверки в методе `check_and_unlock_achievements`.

### Как изменить пакеты Telegram Stars?

Отредактируйте словарь `packages` в функции `buy_callback` в `bot.py`.

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `sudo journalctl -u slot-bot -n 100`
2. Проверьте подключение к БД
3. Убедитесь, что все переменные в `.env` установлены правильно
4. Проверьте, что токен бота действителен

## ⚖️ Лицензия и юридические аспекты

⚠️ **ВАЖНО:**

- Азартные игры регулируются законодательством
- Убедитесь, что ваш бот соответствует законам вашей юрисдикции
- Telegram может заблокировать бота за нарушение ToS
- Используйте только виртуальную валюту для тестирования
- Для коммерческого использования проконсультируйтесь с юристом

## 🎯 Roadmap

Планируемые улучшения:

- [ ] Ежедневные бонусы
- [ ] Реферальная система
- [ ] Турниры и лидерборды
- [ ] Дополнительные игры
- [ ] Web-админка
- [ ] Telegram Mini App интеграция
- [ ] Статистика в реальном времени

## 🤝 Вклад

Этот проект создан для коммерческого использования.

---

**Создано с ❤️ для Telegram**

Удачи в разработке! 🎰✨
