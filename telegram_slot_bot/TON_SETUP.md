# 🔷 TON Интеграция - Инструкция по настройке

## 📋 Что было сделано

Добавлена полная интеграция с TON блокчейном для автоматических выплат выигрышей пользователям.

### ✅ Реализовано:

1. **Модуль `ton_wallet.py`** - работа с TON блокчейном
   - Инициализация кошелька из seed-фразы
   - Отправка TON транзакций
   - Проверка баланса
   - Ожидание подтверждения транзакций (10-30 секунд)

2. **Обновлена база данных**
   - Поле `ton_address` в таблице `users` - TON адрес пользователя
   - Поля для TON транзакций в таблице `transactions`
   - Автоматическая миграция через `add_ton_fields.py`

3. **Обновлен `group_slot_bot.py`**
   - Автоматическая отправка TON при выигрыше
   - Команда `/setaddress` - установить TON адрес
   - Команда `/myaddress` - посмотреть свой адрес
   - Уведомления пользователей и админов

4. **Утилиты**
   - `setup_ton_wallet.py` - создание или импорт TON кошелька
   - `add_ton_fields.py` - миграция базы данных

---

## 🚀 Настройка TON выплат

### Шаг 1: Создание или импорт TON кошелька

Запустите скрипт настройки:

```bash
python setup_ton_wallet.py
```

Выберите:
- **Вариант 1**: Создать новый кошелек (будет сгенерирована seed-фраза)
- **Вариант 2**: Импортировать существующий кошелек (введите свою seed-фразу)

**ВАЖНО:** Сохраните seed-фразу в безопасном месте!

### Шаг 2: Добавление конфигурации в .env

Откройте файл `.env` и добавьте:

```env
# TON Wallet Configuration
TON_WALLET_MNEMONIC="word1 word2 word3 ... word24"
TON_TESTNET=False  # True для тестовой сети, False для mainnet
```

**ВНИМАНИЕ:**
- Никогда не публикуйте `.env` файл в GitHub!
- Seed-фраза дает полный доступ к кошельку!
- Используйте отдельный hot wallet только для выплат!

### Шаг 3: Пополнение кошелька

1. Скопируйте адрес кошелька из вывода `setup_ton_wallet.py`
2. Отправьте TON на этот адрес (минимум 1 TON для комиссий)
3. Рекомендуется держать 5-10 TON для бесперебойных выплат

**Важно:**
- Каждая транзакция стоит ~0.01 TON комиссии
- Держите баланс выше суммы всех возможных выплат + комиссии

### Шаг 4: Запуск бота

```bash
python group_slot_bot.py
```

При запуске вы увидите:
```
Initializing TON wallet...
✅ TON payments enabled! Wallet: UQAbc123...
💎 Wallet balance: 10.5 TON
🎰 Group Slot Bot запущен!
TON PAYMENTS: ✅ ENABLED
```

---

## 💰 Курс конвертации

**По умолчанию:** 1000 stars = 1 TON

Вы можете изменить курс в файле `ton_wallet.py`:

```python
class TONPaymentService:
    # Курс конвертации: 1 Star = X TON
    STARS_TO_TON_RATE = 0.001  # 1 Star = 0.001 TON
```

### Примеры выплат:

| Комбинация | Stars | TON (курс 1:0.001) |
|------------|-------|-------------------|
| 🍒🍒🍒      | 100   | 0.1 TON          |
| 🍋🍋🍋      | 200   | 0.2 TON          |
| 🍇🍇🍇      | 350   | 0.35 TON         |
| 7️⃣7️⃣7️⃣      | 500   | 0.5 TON          |

---

## 👤 Инструкция для пользователей

### Как получать выплаты?

1. **Установите TON адрес:**
   - Отправьте боту в личку: `/setaddress <ваш_TON_адрес>`
   - Пример: `/setaddress UQAbc123defGHI456...`

2. **Где взять TON адрес?**
   - Откройте Tonkeeper, TON Wallet или любой другой TON кошелек
   - Скопируйте адрес для получения

3. **Проверить адрес:**
   - `/myaddress` - показать установленный адрес

4. **Получение выплат:**
   - При выигрыше в группе вы увидите: "Congrats! 💎 TON will be sent soon"
   - Через 10-60 секунд TON придут на ваш адрес автоматически
   - Вы получите уведомление в личке от бота

---

## 🔧 Для разработчиков

### Архитектура

```
group_slot_bot.py
    ↓ при выигрыше
send_ton_payout()
    ↓
ton_wallet.py → TONPaymentService
    ↓
pytoniq → TON Blockchain
    ↓ 10-30 сек
✅ Транзакция подтверждена
```

### Логирование

Все TON транзакции логируются в базу данных (`transactions` таблица):
- `ton_transaction_hash` - hash транзакции в блокчейне
- `ton_amount` - сумма в TON
- `ton_destination` - адрес получателя
- `ton_status` - статус: pending/confirmed/failed

### Обработка ошибок

1. **Недостаточно баланса:**
   - Админы получают уведомление
   - Транзакция помечается как failed

2. **Пользователь без TON адреса:**
   - Пользователь получает инструкцию `/setaddress`
   - Выплата пропускается

3. **Ошибка сети:**
   - Транзакция помечается как failed
   - Админы получают уведомление

---

## 🧪 Тестирование

### Тестовая сеть (Testnet)

Для тестирования используйте testnet:

1. Установите в `.env`:
```env
TON_TESTNET=True
```

2. Получите testnet TON:
   - https://t.me/testgiver_ton_bot
   - Отправьте адрес вашего кошелька

3. Запустите бота и проверьте выплаты

### Тестовый сценарий:

1. Установите TON адрес: `/setaddress <адрес>`
2. Отправьте dice в группу
3. Получите выигрыш (например 🍒🍒🍒)
4. Проверьте:
   - Уведомление в группе: "Congrats! 💎 TON will be sent soon"
   - Уведомление в личке о выплате
   - TON пришли на кошелек (через 10-30 сек)
   - Запись в базе данных

---

## 🛡️ Безопасность

### ⚠️ ВАЖНЫЕ ПРАВИЛА:

1. **Hot Wallet:**
   - Используйте ОТДЕЛЬНЫЙ кошелек только для выплат
   - НЕ используйте основной кошелек с большими суммами
   - Держите минимальный баланс для текущих нужд

2. **Seed-фраза:**
   - Храните в безопасном месте (password manager)
   - НЕ публикуйте в GitHub
   - НЕ передавайте третьим лицам
   - Добавьте `.env` в `.gitignore`

3. **Мониторинг:**
   - Регулярно проверяйте баланс кошелька
   - Следите за уведомлениями о низком балансе
   - Проверяйте логи транзакций

4. **Резервное копирование:**
   - Сохраните seed-фразу в нескольких местах
   - Держите backup базы данных
   - Документируйте все транзакции

---

## 📊 Мониторинг

### Проверка баланса кошелька:

```python
python -c "import asyncio; from ton_wallet import TONWallet; wallet = TONWallet('seed фраза'); asyncio.run(wallet.initialize()); print(asyncio.run(wallet.get_balance()))"
```

### Просмотр TON транзакций в базе:

```sql
SELECT * FROM transactions WHERE transaction_type = 'ton_payout' ORDER BY timestamp DESC LIMIT 10;
```

### Статистика выплат:

```sql
SELECT
    COUNT(*) as total_payouts,
    SUM(ton_amount) as total_ton,
    AVG(ton_amount) as avg_payout,
    ton_status
FROM transactions
WHERE transaction_type = 'ton_payout'
GROUP BY ton_status;
```

---

## 🐛 Troubleshooting

### Проблема: TON payments disabled

**Решение:**
1. Проверьте наличие `TON_WALLET_MNEMONIC` в `.env`
2. Убедитесь что seed-фраза корректна (24 слова)
3. Проверьте логи при запуске бота

### Проблема: Insufficient balance

**Решение:**
1. Проверьте баланс кошелька
2. Пополните кошелек TON
3. Перезапустите бота

### Проблема: Transaction confirmation timeout

**Решение:**
1. Проверьте соединение с интернетом
2. TON сеть может быть загружена - подождите
3. Проверьте статус транзакции в эксплорере: https://tonscan.org

### Проблема: Failed to initialize TON wallet

**Решение:**
1. Проверьте формат seed-фразы (должно быть ровно 24 слова)
2. Убедитесь что установлены библиотеки: `pip install pytoniq tonsdk`
3. Проверьте доступ к интернету

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Проверьте записи в базе данных
3. Проверьте транзакции на https://tonscan.org
4. Проверьте баланс кошелька

---

## 🎯 Следующие шаги

После настройки TON выплат:

1. ✅ Протестируйте на testnet
2. ✅ Пополните mainnet кошелек
3. ✅ Запустите бота в production
4. ✅ Настройте мониторинг баланса
5. ✅ Сообщите пользователям о TON выплатах

**Готово! Теперь бот автоматически отправляет TON выплаты при выигрыше!** 🎉
