# OpenAI
OPENAI_API_KEY=your-openai-api-key-here

# Encryption key для токенов HH.ru (генерируется автоматически если не указан)
# Для генерации используйте: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=