# БОТ ДЛЯ СБОРА СТАТИСТИКИ DICE

Этот бот отправляет много dice (🎰) и собирает статистику по выпавшим значениям.
Это необходимо для определения правильного RTP (Return to Player) в основном боте.

## Установка

1. Создайте нового бота через @BotFather
2. Скопируйте токен бота
3. Откройте `.env` и вставьте токен в `BOT_TOKEN`
4. Установите зависимости:

```bash
pip install -r requirements.txt
```

## Как использовать

1. Запустите бота:
```bash
python dice_collector.py
```

2. Напишите боту в Telegram команду `/start`

3. Отправьте команду `/collect` для начала сбора статистики

4. Дождитесь завершения (займет ~8-10 минут для 1000 бросков)

5. Получите файлы:
   - `dice_stats_YYYYMMDD_HHMMSS.csv` - все результаты в CSV
   - `dice_report_YYYYMMDD_HHMMSS.txt` - отчет с анализом

## Что делает бот

1. Отправляет N бросков dice (🎰) (по умолчанию 1000)
2. Записывает каждое значение (1-64) в CSV файл
3. Анализирует частоты выпадения каждого значения
4. Создает отчет с процентами и статистикой
5. Отправляет отчет и CSV файлы в Telegram

## Настройки в .env

- `BOT_TOKEN` - токен бота от @BotFather
- `ADMIN_CHAT_ID` - ваш chat_id (517682186)
- `ROLLS_COUNT` - количество бросков для сбора (по умолчанию 1000)

## Использование результатов

После сбора статистики вы получите:
- Точные вероятности каждого значения dice
- Частоты выпадения
- Данные для расчета правильного RTP

Эти данные помогут настроить таблицу выплат в основном боте @starsburstbot
